[project]
name = "voice-service"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.40",
    "google-genai>=2.18.1",
    "langsmith>=0.11.1",
    "livekit-agents[google]>=1.6.8",
    "pydantic-settings>=2",
    "redis>=8.1.0",
    "sentry-sdk>=2.67.1",
    "voice-contract",
    "websockets>=14.0,<17.0",
]

[project.optional-dependencies]
# Opt-in: gives voice-service a local, agentkit-backed fallback for when
# CollectiveOS is unreachable (see local_agent.py). Not a hard dependency
# -- agentkit lives as a sibling checkout outside this monorepo, so
# requiring it unconditionally would break `uv sync` for anyone who
# clones this repo without also having agentkit checked out at that
# exact relative path.
local-agent = ["agentkit[gemini]"]

[project.scripts]
voice-service = "voice_service:main"

[build-system]
requires = ["uv_build>=0.11.18,<0.12.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "mock-agent-backend",
    "pytest>=9.1.1",
]

[tool.uv.sources]
voice-contract = { path = "../../libs/voice-contract", editable = true }
mock-agent-backend = { path = "../mock-agent-backend", editable = true }
agentkit = { path = "../../../agentkit", editable = true }
